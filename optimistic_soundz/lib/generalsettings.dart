import 'package:flutter/material.dart';

class General_Settings extends StatelessWidget {
  const General_Settings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("General Settings")),
    );
  }
}
